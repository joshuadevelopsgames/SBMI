---
description: Restrict edits to the SBMI portal app (deploy target /var/www/sbmi.ca)
globs: "**/*"
alwaysApply: true
---

# SBMI scope: portal codebase at repo root

- **Only create, edit, or delete files inside this repository’s workspace root** (the Next.js app that deploys to `/var/www/sbmi.ca`).
- Do not modify unrelated sibling repos or paths outside this project unless the user explicitly asks.
- Deploy/config under `deploy/` is in-scope for production setup.
