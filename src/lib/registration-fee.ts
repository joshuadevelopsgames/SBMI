/** Placeholder amounts until Stripe + per-category fees (Phase 1c) ship. */
export const REGISTRATION_FEE_CAD = 150;
export const REGISTRATION_GST_RATE = 0.05;
export const REGISTRATION_TOTAL_CAD = REGISTRATION_FEE_CAD * (1 + REGISTRATION_GST_RATE);
